/*生成后的脚本存放的位置,如果填写盘符则为绝对路径不填写则为当前活动表格的相对路径*/
SaveScriptPath = Script/,
/*生成后的数据库存放位置,如果填写盘符则为绝对路径不填写则为当前活动表格的相对路径*/
SaveDbPath = DB/,
/*生成的脚本的命名空间*/
ScriptNameSpace = SuperHero.Config,
/*文件后缀名带有该后缀名的可以享有该插件的所有能力*/
FileSuffix = _Auto,
/*枚举类型后缀*/
EnumSuffix = Enum,
/*生成的数据库类型   目前支持DB,Json*/
GeneratorType = Json,


